# Changelog

## 0.1.15

### 破壊的変更
- **全日程を一括で自動配置する機能を廃止**。自動配置は日程ごとに実行する。`POST /api/assignments/auto-assign` は `target_date`（YYYY-MM-DD）が必須になり、日付指定なし・複数日にまたがる `session_ids` の指定は 400 を返す

### バグ修正
- 担当スタッフの選択ボックスに、活動可能時間外のスタッフが候補として表示される問題を修正。自動配置と同じ条件（セッションの開始〜終了が活動可能時間の枠に収まること。活動可能時間が未設定なら制約なし）で絞り込むように
- 自動配置で全日程を通した負荷平準化と max_hours 制約が強すぎて、配置可能なスタッフがいるのに「人員不足」で配置に失敗する問題を修正。max_hours・負荷均等化・連続配置などの制約は日単位で適用し、日またぎの偏りはソフトな減点として考慮
- 自動配置がタブごとの範囲（セッション担当・受付案内・懇親会など）しか対象にしておらず、「配置完了」と表示されるのに他カテゴリが未配置のまま大量に残る問題を修正。日付を選んで実行すると、その日の全カテゴリのセッションがまとめて配置される
- 「配置完了 N/M」の分母に、配置対象外のセッション（必要人数0・「全員」設定・全体スケジュール）が含まれ充足率が水増しされていた問題を修正。配置対象外は `skipped_sessions` として分離し、実際に配置した人数 `assigned_slots`/`required_slots` も返すように
- 必要人数が0のセッションに「未配置」バッジが表示され続ける問題を修正。「配置不要」と表示するように

### 機能追加
- **PWA 対応**。ホーム画面に追加でき、オフラインでも閲覧できるように
  - 一度オンラインで開けば、電波が無くてもマイページ・全体スケジュール・スタッフ別詳細・担当表・会場地図・顔写真が読める
  - 起動時に全画面ぶんのアセット・読み取り API 一式・フロアマップ画像をまとめて保存するため、オンラインのときに開いていない画面もオフラインで表示できる
  - オフラインでの追加・編集・削除は保存されない（編集を溜めて後から送る仕組みは持たない）
  - `/manifest.webmanifest` はアプリタイトルを反映して配信。アイコン・Service Worker とあわせて認証を通さない（ブラウザが cookie 抜きで取得するため、認証の内側だとインストールできない）
- 通信が切れているあいだ、画面右下に「オフライン / 最終更新: x分前」のトーストを表示。30秒ごとに自動で再接続を試み、復帰すると最新に追いついて短く知らせる（「再試行」で即時実行も可能）
- 自動配置ボタンは日程タブで日付を選ぶまで無効化し、未選択時は案内を表示

### 開発
- 自動配置のテストを追加（`tests/`）。`scheduler.db` がある場合は本番データに対する回帰テストも実行される

## 0.1.14

### 改善
- 自動配置でスコアが同点の場合、担当件数・合計担当時間が少ないスタッフを優先
- まだ1件も担当していないスタッフに加点（+30）し、担当が全員に行き渡るように

## 0.1.13

### バグ修正
- 配置表で開始時刻が15分刻みでない日に時間ラベルが表示されない問題

## 0.1.12

### 改善
- 自動配置の負荷均等化を担当件数と累計担当時間のハイブリッドに変更（担当件数 -8/件、担当時間 -12/時間）。長時間シフトが軽く扱われる問題と、希望セッションの多いスタッフへの偏りを抑制

## 0.1.11

### 機能追加
- 全体スケジュール用に削除不可の「全体」部屋を導入（部屋一覧・選択肢からは非表示）
- 別部屋への移動時間（既定10分）を導入し、設定で分数を変更可能に。自動配置・手動配置・候補表示・ドラッグ移動のすべてで考慮

### 改善
- スケジュールの配置表を固定幅の等幅列にし、高さをビューポートに追従・ヘッダーと時間列を固定
- 日別表示ではその日に使う部屋のみを列表示
- 本体を画面幅いっぱいの白いフラット表示に変更（余白・カード装飾・グレー背景を廃止）
- 全日程タイムライン・管理一覧の時間を1行表示に統一
- スケジュールの詳細表示を常に読み取り専用化（ロック切替を非表示）
- 日付タブからセッショングループ名の付加表示を削除
- スタッフの希望セッションを1行1件・時間付きで表示
- 必要人数どおりに配置（1なら1名）、必要人数0は配置対象外
- 配置アルゴリズムに「未経験のみ」解消のスワップ処理でも移動時間制約を適用

### バグ修正
- 「全体」という部屋名の作成・改名を禁止（予約名）
- Excelシート名の重複回避

## 0.1.10

### バグ修正
- 必要人数を0に設定したセッションに自動配置でスタッフが割り当てられる問題

## 0.1.9

### 機能追加
- 配置に「未配置を埋める」ボタンを追加（既存配置を維持し不足分のみ配置）
- 配置アルゴリズムに連続配置・同部屋連続・同一ペア重複の回避を追加
- 別部屋への配置に移動時間10分を必須条件として追加

### 改善
- Excel出力を日別シートに分割（セッション管理・セッション配置・全体スケジュール・カテゴリ別）
- Excelのシートを種類ごとにまとめ、各種類内で日付順に配置
- Excelスタッフ管理の活動可能時間に日付を追加

### バグ修正
- セッション形式（設定で追加したカテゴリ）のセッションがExcelに出力されない問題

## 0.1.8

### 改善
- 全日程タイムラインのスマホ表示を1行のコンパクト表示に変更（列幅統一・タイトル省略・横スクロール対応）

## 0.1.7

### 機能追加
- 設定に「担当管理」を追加（カスタム担当の追加・編集・削除）
- カテゴリ・セッショングループにカスタム担当を紐づけ可能に（紐づけた担当も配置対象）
- 手動配置にロール検証を追加（担当外のスタッフは配置不可、全体スケジュールは全員可）
- 写真登録にクリップボード貼り付けを追加（スタッフ・登壇者・LT登壇者・会場地図）

### 改善
- 担当未選択のスタッフは「なし」と表示、新規登録時のデフォルトは未選択
- スタッフ別詳細の絞り込みを名前から担当に変更
- スタッフ別詳細の「担当セッションなし」を「担当なし」に変更
- カテゴリ管理・セッショングループ管理に紐づく担当をバッジ表示
- 「セッションカテゴリ管理」を「セッション形式管理」に改名
- セッション形式から「技術」を廃止
- Excelスタッフシートの担当列をラベル表示に修正（複数担当・なし対応）

### バグ修正
- セッション管理・セッショングループ管理でセッション編集後に配置表が更新されない問題
- 担当ドロップダウンでチェックすると閉じて選択できない問題
- 設定のセッション形式一覧にLT・パネルディスカッションが表示されない問題
- カテゴリ担当・セッション詳細モーダルの候補スタッフが常にセッション担当で絞られる問題

## 0.1.6

### 機能追加
- 管理パネル（全体スケジュール・セッショングループ・カテゴリ）に配置表を追加
- 配置表でのドラッグ&ドロップによるセッション移動
- 配置表のセッションクリックでアクションメニュー（詳細・編集・削除）を表示

### 改善
- 必要スタッフ計算をセッショングループ管理から担当パネルに移動
- Excelエクスポートの全体スケジュールを5分刻みに変更
- 全体スケジュールのセルに登壇者名を追加表示

## 0.1.4

### 機能追加
- セッショングループ管理パネルに必要スタッフ計算ボタンを追加

### 改善
- セッション担当パネルの全日程表示をカテゴリ担当と同じ3カラム形式に統一
- 「選択して再配置」ボタンに選択件数を表示
- スタッフフィルター時にフィルター件数を表示
- 「配置マトリクス」を「配置表」に名称変更
- 1日のみのイベントでも日程タブを表示

### バグ修正
- LT・パネルディスカッション作成時に登壇者名が未入力として拒否される問題
- セッション担当タブで全日程が初期選択されない問題

## 0.1.3

### セキュリティ
- ログインパスワード・管理者パスワードをPBKDF2-SHA256でハッシュ化して保存
- 既存の平文パスワードはログイン成功時に自動でハッシュへ移行

### 機能追加
- パブリッシュ時のWebhook通知（任意のURLへPOST送信）
- GitHub Actions連携（workflow_dispatchによるワークフロー自動実行）
- スケジュールAPIのURLをworkflow_dispatch inputsとして自動送付
- GitHub Personal Access Tokenのクリア機能

### 改善
- 公開APIタブの使い方ガイドを更新（Dispatch URL取得方法、PAT権限、ワークフロー例）
- 利用方法に公開APIセクションを追加、推奨ワークフローにステップ10を追加
- 入力フィールドの縦幅を改善
- UI表記を「発火」から「実行」に統一

## 0.1.2

### 機能追加
- 公開API（スナップショット方式によるスケジュールJSON配信）
- APIキー認証、CORS設定、パブリッシュ履歴管理
- 登壇者写真のAPIキー認証付き配信
- LT・パネルディスカッションの代表者（is_representative）機能
- 編集中アイテムのハイライト表示、編集ボタンの「編集中」状態表示

### 改善
- 写真アップロード処理を共通ヘルパーに統合
- 公開APIのDB問い合わせをバッチ化
- 重複関数（fmtTime, autoSetLTEndTime）を統合
- タブ切り替え時のデータ読み込みを並列化（Promise.all）
- 未使用コード削除（staff_skills テーブル宣言、冗長なセッション存在チェック）

### バグ修正
- is_representativeがNULLの場合にセッションAPI 500エラー
- 写真アップロードリファクタ後のPath import欠落

## 0.1.1

### 機能追加
- 初期設定フロー（ログインパスワード・管理者パスワード・タイトル・タイムゾーン）
- タイムゾーン設定（Azure UTC問題の解消）
- パネルディスカッションのExcelエクスポート対応
- サンプルデータ同梱
- フォームバリデーション強化

### 改善
- アプリ名をEvent Schedulerに変更、DB名をscheduler.dbに変更
- MITライセンス追加
- 自動バックアップのステータス表示をリアルタイム更新（1秒ポーリング）
- サイドバーのタブ名・順序を整理
- Excelエクスポートのファイル名プレフィックスを変更

### バグ修正
- 自動バックアップのnext_runが表示されない問題
- カテゴリセッション作成時にdate文字列がgroup_idとして送信される問題

## 0.1.0

### 初期リリース
- セッション管理（通常・LT・パネルディスカッション）
- スタッフ管理（ロール・英語対応・稼働時間・経験・希望セッション）
- 部屋管理・会場地図
- セッショングループ・動的カテゴリ
- 全体スケジュールマトリクス（タイムライン表示）
- スタッフ自動配置アルゴリズム（スコアリング方式）
- スタッフ別詳細表示
- ドラッグ&ドロップによるセッション移動
- Excelエクスポート・connpassテンプレート出力
- バックアップ・復元・自動バックアップ
- パスワード認証・GeoIP制限・レート制限・セキュリティヘッダー
- モバイルレスポンシブ対応
